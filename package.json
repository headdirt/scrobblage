{
  "name": "scrobblage-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "build:standalone": "node scripts/build-standalone.js"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
